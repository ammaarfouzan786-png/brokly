# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/

# Build output
dist/
build/
.next/
.next-stale/
.next-bak*/
.next-old*/
out/
coverage/
*.tsbuildinfo

# Environment & secrets
.env
.env.*
!.env.example
secrets/
*.pem
*.key

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Database
*.sqlite
*.sqlite3
prisma/*.db

# Large binaries / archives (too big or unnecessary for GitHub)
*.zip
*.mp4
*.mov

# Prior-attempt reference folder — it's a SEPARATE nested git repo with its own
# .env.local secrets + large media. Excluded from this repo; push it on its own.
whatsapp-integration-try/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# Test & cache
.cache/
.turbo/
.eslintcache

# Claude Code local overrides (keep shared config, ignore personal)
.claude/settings.local.json

# Playwright MCP session captures (screenshots/snapshots from agent runs)
.playwright-mcp/
